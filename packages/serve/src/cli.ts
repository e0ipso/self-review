#!/usr/bin/env node

// Placeholder entry point so the package has something to build and later tasks
// have somewhere to write. Argument parsing, the review session and the HTTP
// listener arrive in subsequent tasks of this plan.
process.exit(0);

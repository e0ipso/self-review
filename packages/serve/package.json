{
  "name": "@self-review/serve",
  "version": "1.43.4",
  "description": "Serve the self-review interface over HTTP",
  "repository": {
    "type": "git",
    "url": "https://github.com/e0ipso/self-review.git",
    "directory": "packages/serve"
  },
  "publishConfig": {
    "access": "public"
  },
  "type": "module",
  "bin": {
    "self-review-serve": "./dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "prebuild": "npm run build --workspace @self-review/core && npm run build --workspace @self-review/react",
    "build": "tsup && npm run build:client",
    "build:client": "vite build --config vite.client.config.ts",
    "dev": "tsup --watch",
    "test:unit": "vitest run"
  },
  "dependencies": {
    "@self-review/core": "^1.43.4",
    "@self-review/react": "^1.43.4"
  },
  "devDependencies": {
    "tsup": "^8.0.0",
    "typescript": "~5.9.0",
    "vite": "^7.3.1",
    "vitest": "^4.0.18"
  },
  "license": "MIT"
}
